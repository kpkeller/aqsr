context("test-service_checks")

test_that("incorrect service fails", {
    expect_error(checkService("qa"), "Service ")
})

context("test-service_checks")

test_that("incorrect service fails", {
    expect_error(checkService("qa"), "Service ")
})
